<% content_for :header do %>
	<h1><%= @title = 'Editing ' + @user.name %></h1>
<% end %>

<%= render 'form' %>

<br />

<div class="bottom_nav">
<%= link_to image_tag('show.svg', :class => 'action') + ' Show Clicker', @user %> |
<%= link_to image_tag('back.svg', :class => 'action') + ' To Clicker List', users_path %>
</div>