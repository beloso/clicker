<div class="clicker_form">
<%= simple_form_for(@admin) do |f| %>
  <% if @admin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @admin.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  	<%= f.input :name, :hint => 'Admin\'s Username'%>
  	<%= f.input :email, :placeholder => 'user@domain.com', :hint => 'Admin\'s Email'%>
    <%= f.input :password,:hint => 'Admin\'s password'%>
    <%= f.button :submit %>
  </div>
<% end %>
</div>