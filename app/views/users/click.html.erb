<h1>Clicking <%= @user.name %></h1>

<p id="notice"><%= notice %></p>

<%= next_user_button %>
<%= end_clicking_button %>
<%= @selected_user = session[:current_user];
	@selected_user.name %>

<iframe src=<%= @user.url %> width="100%" height="600">
  <p>Your browser does not support iframes.</p>
</iframe>